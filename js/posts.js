// js/posts.js —— 笔记清单（新增笔记：1) 新建 content/posts/<slug>.md；2) 在此数组加一条）
const POSTS = [
  {
    slug: "breast-ultrasound-review",
    title: "乳腺超声图像诊断系统：35 次实验后的复盘",
    date: "2026-06-15",
    tags: ["项目复盘", "PyTorch", "医学影像"],
    file: "content/posts/breast-ultrasound-review.md",
    content: `---
title: "乳腺超声图像诊断系统：35 次实验后的复盘"
date: 2026-06-15
tags: ["项目复盘", "PyTorch", "医学影像"]
---

# 乳腺超声图像诊断系统：35 次实验后的复盘

上学期和同学组队做乳腺超声图像的良恶性分类。超声影像和病理切片不同：噪声多、病灶边界模糊、标注成本高，是典型的"小数据 + 高噪声"任务。我们选了 PyTorch，从 ResNet 迁移学习起步，目标是对一张 224×224 的灰度图输出"良性 / 恶性"。

## 背景

选这个题的原因是直白：家里有亲戚做过乳腺检查，报告上的分级用语我们看不懂，就想着"能不能训练一个模型先给个参考"。后来才知道，医学影像模型离能用还有很远——但这是后话。

## 挑战

1. **数据少**：训练集只有 800 多张图，直接微调很快就过拟合。
2. **类别不平衡**：恶性样本只占三成左右。
3. **病灶区域小**：ROI 常常只占整张图的十分之一，全局特征会把局部细节淹没。
4. **经验为零**：我们俩都是第一次做医学影像，前两周大半时间花在装环境、对齐数据格式上。

## 方案

- **迁移学习 + 分层微调**：冻结骨干网络前 60% 层，只训练后半段和分类头；
- **在线增强**：随机旋转、平移、gamma 变换、轻度弹性变形；
- **Focal Loss** 替代 CrossEntropy，缓解类别不平衡；
- **CBAM 注意力模块**，让网络学会"看哪里"；
- **三折交叉验证 + TTA**，报告平均指标。

最终在验证集上拿到 **AUC 0.9225**、准确率 0.871。成绩不算顶尖，但整个过程把"实验纪律"练出来了——这比分数更值钱。

## 教训

1. **先搭评估管线，再调模型。** 我们前两版"对比"其实是在不统一的验证协议上比大小，前 10 次实验基本白做。固定的 seed、固定的划分、固定的增强开关——这些无聊的东西先定死，后面的数字才有意义。
2. **记录比代码重要。** 从第 11 次实验开始用表格记录：seed、学习率、增强开关、指标。没有记录对比实验等于没做，半年后连自己都说不清"那次怎么涨的"。
3. **小模块比大模型管用。** 同一个骨干网络，加 CBAM 的提升比 ResNet18 换成 ResNet34 更大。对小目标、小数据，注意力比深度更值钱。
4. **数据私有，项目形态就得跟着变。** 这批影像不能公开，所以仓库目前以实验文档和笔记为主（状态：进行中）。发布不了的代码更要写好 README——半年后的自己，也是这个仓库的"陌生人"。

## 下一步

- 补全数据增强与 Focal Loss 的消融实验；
- 试一下"SAM 先分割、再分类"的两阶段流程；
- 把实验表格整理成公开笔记，供同方向的同学参考。
`
  },
  {
    slug: "c-language-notes",
    title: "C 语言学习笔记：指针，从数组到函数",
    date: "2026-07-20",
    tags: ["C语言", "学习笔记"],
    file: "content/posts/c-language-notes.md",
    content: `---
title: "C 语言学习笔记：指针，从数组到函数"
date: 2026-07-20
tags: ["C语言", "学习笔记"]
---

# C 语言学习笔记：指针，从数组到函数

用惯了 Python 再学 C，指针是第一个坎。这份笔记按"为什么要这样"来写，而不是只抄语法。

## 指针就是地址

变量的值存在内存里，\`&a\` 取出它的地址；指针就是存放地址的变量：

\`\`\`c
#include <stdio.h>

int main(void) {
    int a = 42;
    int *p = &a;         // p 里存的是 a 的地址

    printf("a = %d\\n", a);    // 42
    printf("*p = %d\\n", *p);  // 42，*p 是解引用
    *p = 100;                 // 通过指针修改 a
    printf("a = %d\\n", a);    // 100
    return 0;
}
\`\`\`

## 数组名会"退化"成指针

数组名在表达式中会被当作指向首元素的指针，所以 \`arr[i]\` 等价于 \`*(arr + i)\`：

\`\`\`c
int arr[4] = {1, 2, 3, 4};
int *q = arr;          // 不需要 &，arr 本身退化为指针
printf("%d\\n", *(q + 2)); // 3
\`\`\`

两个易错点：

- \`sizeof(arr)\` 在数组定义处返回整个数组的字节数；一旦传进函数，就只剩指针的 8 字节（64 位平台）；
- 指针加法移动的单位是"所指类型的大小"，不是字节——\`q + 1\` 跳过的是 4 个字节（对 \`int\` 而言）。

## 函数参数：传值还是传指针

C 的参数一律传值。想让函数修改调用方的变量，就要传地址：

\`\`\`c
void swap(int *x, int *y) {
    int t = *x;
    *x = *y;
    *y = t;
}
\`\`\`

## 动态内存：malloc 与配对的 free

\`\`\`c
#include <stdlib.h>
#include <stdio.h>

int main(void) {
    int *nums = malloc(10 * sizeof(int));
    if (nums == NULL) {        // 分配失败返回 NULL
        perror("malloc");
        return 1;
    }
    for (int i = 0; i < 10; i++)
        nums[i] = i * i;
    free(nums);                // 和 malloc 配对，忘了就是内存泄漏
    return 0;
}
\`\`\`

## 三条经验

> 先把"变量、指针、内存"画成方框和箭头，再看代码，大部分混淆会自己消失。

1. **先画图再写代码**——手画一张内存图，胜过十遍报错；
2. **把编译器的警告当 bug 查**——\`-Wall -Wextra\` 全开，零警告才往下走；
3. **valgrind 是内存课的老师**——\`valgrind ./a.out\` 直接告诉你哪里越界、哪里泄漏。

下一步：用单向链表重写 Favorite-sticker 里剪贴板历史的存储结构，亲身感受手动管理内存和垃圾回收的区别。
`
  },

  {
  slug: "test",
  title: "笔记标题测试",
  date: "2026-08-10",
  tags: ["标签1", "标签2"],
  file: "content/posts/test.md",
  // content 字段可以不填！线上环境会自动从 file 加载。
  }
];

window.POSTS = POSTS;
